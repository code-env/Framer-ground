---
title: Create
description: Create button effect when the client's click on the create new.
author: code-env
---

<ComponentPreview name="create-new-button" />

## Installation

<Steps>

<Step>Run the following command</Step>

It will create a new file `create.tsx` inside the `components/buttons/create.tsx` directory.

```bash
mkdir -p components/buttons && touch components/buttons/create.tsx
```

<Step>Paste the code</Step>{" "}

Open the newly created file and paste the following code:

```jsx jsx file=<rootDir>/fg/button/create-new.tsx

```

<Step>Usage</Step>

In the directory you want to use, do the following:

```jsx {1} {8}
import Create from "@/components/buttons/create";

const MyButtons = () => {
  return (
    <div>
      {/* your other content*/}

      <Create text="Create" />

      {/* your other content*/}
    </div>
  );
};

export default MyButtons;
```

</Steps>

## Credits

Built by [Bossadi Zenith](https://github.com/code-env)
